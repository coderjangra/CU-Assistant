services:
  - type: web
    name: cu-chatbot-backend
    env: python
    rootDir: cu-chatbot-backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    plan: free
